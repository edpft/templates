# `use flake` already reloads on flake.nix and flake.lock, but not on the
# toolchain file those two derive the Rust version from.
watch_file rust-toolchain.toml

use flake

# Project-local secrets and settings, kept out of git.
dotenv_if_exists .env
