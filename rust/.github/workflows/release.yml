name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: edpft/rust-workflows/.github/workflows/release-please.yml@main
